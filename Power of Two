import math
class Solution:
    def isPowerOfTwo(self, n: int) -> bool:
        if(n<=0):
            return False
        x=int(math.log(n,2))
        y=int(math.pow(2,x))
        if(n==y):
            return True
        else:
            return False
        
        
        
